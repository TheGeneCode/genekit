version: 2
updates:
  # The workflow SHA-pins its actions; without this the pins silently rot.
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "monthly"
    open-pull-requests-limit: 2
    commit-message:
      prefix: "ci"
